<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.mappers.board">
	
	<!-- added site set -->
<select id="list" resultType="com.board.domain.BoardVO">
 select
 USERID, url, sitename, keyword
 from tbl_site_add
 where USERID = #{USERID}
</select>


<!-- add site -->
<insert id="write" parameterType="com.board.domain.BoardVO">
 insert into
  tbl_site_add(USERID, url, sitename, keyword)
   values(#{USERID}, #{url}, #{sitename}, #{keyword})
</insert>

	<!-- 크롤링 데이터 테이블에 표시 -->
<select id="datalist" resultType="com.board.domain.CrawlVO">
 select
  div1, div2, div3
 from tbl_site
</select>

<!-- 등록 사이트 정보 조회 -->
<select id="view" parameterType="int" resultType="com.board.domain.BoardVO">
 select 
  USERID, url, sitename, keyword
 from 
  tbl_site_add
 where
  USERID = #{USERID}   
</select>

<!-- 등록사이트 수정 -->
<update id="modify" parameterType="com.board.domain.BoardVO" >
 update tbl_site_add
  set
   url = #{url},
   sitename = #{sitename},
   keyword = #{keyword}
  where USERID = #{USERID} 
</update>



</mapper>